spring:
  main:
    allow-bean-definition-overriding: true

  config:
    import: optional:file:.env[.properties]

  datasource:
    # 💡 Linux 환경(CI)을 위해 URL 설정을 조금 더 보강합니다.
    url: jdbc:h2:mem:testdb;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;CASE_INSENSITIVE_IDENTIFIERS=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    # 💡 1. 스프링 레벨에서 H2 사용을 명시
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      # 💡 2. 하이버네이트 레벨에서 방언(Dialect)을 강제로 고정 (가장 확실한 해결책)
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true

  # 💡 Redis 의존성 때문에 서버가 안 뜨는 것을 방지
  data:
    redis:
      host: localhost
      port: 6379

# 아래 보안 및 API 설정은 기존과 동일하게 유지하세요.
security:
  jwt:
    secret: "ci_test_secret_please_use_a_long_random_string_1234567890_abcdefghijklmnopqrstuvwxyz"

api:
  welfare:
    service-key: "ci_dummy_api_key"

solapi:
  api-key: "ci_dummy_api_key"
  api-secret: "ci_dummy_api_secret"
  sender-number: "01000000000"

# Twilio 설정 (CI 더미 값)
twilio:
  account-sid: "ci_dummy_account_sid"
  auth-token: "ci_dummy_auth_token"
  verify-service-sid: "ci_dummy_verify_sid"
  messaging-service-sid: "ci_dummy_messaging_sid"

# LUXIA 설정 (CI 더미 값)
luxia:
  api-key: "ci_dummy_luxia_key"

# Chatbot 설정 (CI 더미 값)
chatbot:
  python:
    url: "http://localhost:5000"
  secret:
    key: "ci_dummy_chatbot_key"

# Callbot 설정 (CI 더미 값)
callbot:
  api:
    url: "http://localhost:5000"

# AWS 설정 (CI에서는 S3 비활성화)
cloud:
  aws:
    credentials:
      access-key: "ci_dummy_access_key"
      secret-key: "ci_dummy_secret_key"
    region:
      static: "ap-northeast-2"
    s3:
      bucket: "ci-test-bucket"
    stack:
      auto: false